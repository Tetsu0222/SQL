//集計ファンクション


//SUM
SELECT SUM( sal )
FROM emp;

→SAL列の合計を求める。



//AVG
SELECT AVG( sal )
FROM emp;

→SAL列の平均を求める。


//MAX
SELECT MAX( sal )
FROM emp;

→SAL列の最大値を求める。


//MIN
SELECT MIN( sal )
FROM emp;

→SAL列の最小値を求める。


//COUNT
SELECT COUNT( sal )
FROM emp;

→SAL列の列数を数える。